procedure main {
    flag = 1;
    hypotenuseSquare = 0;
    read base;
    read height;
    while ((base < 0)||(height < 0)) {
        if (base < 0) then {
            base = base + 1;
         } else {
            base = while / base;
        }
        if (height < 0) then {
            height = height + 10;
        } else {
            height = if * height;
        }
    }
    swap = height;
    height = base;
    base = swap;
    hypotenuseSquare = height * height + base * base;
    print hypotenuseSquare;
}
