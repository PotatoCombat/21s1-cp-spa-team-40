1 - 2 pattern 2 such that one common
assign a1, a2, a3; print p1, p2;
Select a1 pattern a1("x", _) and a2("y", _) such that Follows*(a3, 6) and Follows(p1, p2)

5000
2 - 2 pattern 2 such that one common written differently
assign a1, a2, a3; print p1, p2;
Select a1 pattern a1("x", _) pattern a2("y", _) such that Follows*(a3, 6) and Follows(p1, p2)

5000
3 - 2 pattern 2 such that one common written differently
assign a1, a2, a3; print p1, p2;
Select a1 pattern a1("x", _) pattern a2("y", _) such that Follows*(a3, 6) such that Follows(p1, p2)

5000
4 - 2 pattern 2 such that one common written differently invalid
assign a1, a2, a3; print p1, p2;
Select a1 pattern a1("x", _) and Follows(p1, p2) pattern a2("y", _) such that Follows*(a3, 6)

5000
5 - 2 pattern 2 such that one common invalid
assign a1, a2, a3; print p1, p2;
Select a1 pattern a1("x", _) such that Follows*(a3, 6) and Follows(p1, p2) and a2("y", _)

5000
6 - 2 pattern 2 such that two common
assign a1, a2, a3; print p1, p2;
Select a1 pattern a1("x", _) and a2("y", _) such that Follows*(a1, 6) and Follows(p1, p2)

5000
7 - 3 pattern one parent one common
assign a1, a2; if ifs1, ifs2; while w1, w2;
Select w1 such that Parent(w1, a1) pattern a1("x", _"5"_) pattern ifs1(_, _, _) such that Parent(w2, ifs2)

5000
8 - 3 pattern one parent two common
assign a1, a2; if ifs1, ifs2; while w1, w2;
Select w1 such that Parent(w1, a1) pattern a1("x", _"5"_) pattern ifs1(_, _, _) such that Parent(w2, ifs1)

5000
9 - 3 pattern one parent two common
assign a1, a2; if ifs1, ifs2; while w1, w2;
Select <w1, ifs1> such that Parent(w1, a1) pattern a1("x", _"5"_) pattern ifs1(_, _, _) such that Parent(w2, ifs1)

5000
10 - calls + next + two pattern
call c; while w; procedure p1, p2; assign a; if ifs;
Select c such that Calls(p1, p2) and Next(w, c) pattern a(_, _) and ifs("x", _, _)

5000
11 - calls + next + two pattern
call c; while w; procedure p1, p2; assign a; if ifs;
Select <p1, p2> such that Calls(p1, p2) and Next(w, c) pattern a(_, _) and ifs("x", _, _)

5000
12 -  2 follows + 2 pattern
assign a1, a2, a3; print p1, p2; call c;
Select <a1, a2, a3> such that Follows(a3, 13) pattern a1("x", _) and a2("c", _) such that Follows(p1, p2)

5000
13 - 2 pattern + parent + follows
assign a1, a2; if ifs1, ifs2; while w1, w2, w3;
Select w2 such that Parent(w1, a2) pattern a1("a", "b") such that Follows(ifs2, w2) pattern ifs1(_, _, _)

5000
14 - 2 pattern + calls + next
call c; while w; procedure p1, p2, p3; assign a; if ifs;
Select p3 such that Calls(p1, p2) and Next(w, c) pattern a(_, _) and ifs("x", _, _)

5000
15 - 2 pattern + calls + next
call c; while w; procedure p1, p2, p3; assign a; if ifs;
Select <p1, p2, p3> such that Calls(p1, p2) and Next(w, c) pattern a(_, _) and ifs("x", _, _)

5000
16 - 2 pattern + calls + next
call c; while w; procedure p1, p2, p3; assign a; if ifs;
Select <p1, p2, p2, p3> such that Calls(p1, p2) and Next(w, c) pattern a(_, _) and ifs("x", _, _)

5000
17 - 2 pattern + calls + next
call c; while w; procedure p1, p2, p3; assign a; if ifs;
Select <w, c, ifs> such that Calls(p1, p2) and Next(w, c) pattern a(_, _) and ifs("x", _, _)

5000
18 -  2 pattern + parent + follows
assign a; if ifs; while w; read r;
Select w pattern a("y", _) such that Parent(ifs, a) pattern a(_, _"a"_) such that Follows(r, a)

5000
19 -  2 pattern + parent + follows
assign a; if ifs; while w; read r;
Select r pattern a("d", _) such that Parent(ifs, a) pattern a(_, _"w"_) such that Follows(a, w)

5000
20 - 1 pattern + 2 uses
assign a; if ifs; variable v; call c; read r;
Select c such that Uses(a, v) and Uses(r, v) pattern ifs(v, _, _)

5000
21 - 2 pattern + uses + next
procedure p; if ifs; variable v; assign a;
Select p such that Next(a, ifs) pattern ifs("a", _, _) pattern ifs("c", _, _) such that Uses(ifs, v)

5000
22 - 2 pattern + parent* + follows
assign a; if ifs; read r; call c;
Select ifs pattern a("w", _) such that Parent*(ifs, a) pattern a(_, _"b"_) such that Follows(a, c)

5000
23 - edge case
stmt BOOLEAN;
Select BOOLEAN such that Follows(1, 2)
ALL_STMTS
5000
24 - all stmts
stmt s;
Select s;

5000
25 - Uses edge case

Select BOOLEAN such that Uses("B", "c")
TRUE
5000
26 - Modifies edge case

Select BOOLEAN such that Modifies("B", "a")
TRUE
5000
27 - Uses edge case

Select BOOLEAN such that Uses(23, "x")
TRUE
5000
28 - Modifies edge case

Select BOOLEAN such that Modifies(23, "y")
TRUE
5000
29 - 2 pattern + uses + modifies
assign a1, a2; if ifs; while w; variable v; call c; print r1, r2;
Select a1 such that Uses(r1, v) such that Modifies(a1, v) pattern ifs(v, _, _) and a2(v, _)

5000
30 - 2 pattern + uses + modifies + with
assign a1, a2; if ifs; while w; variable v; call c; print r1, r2;
Select <a1, a2> such that Uses(r1, v) such that Modifies(a1, v) pattern ifs(v, _, _) and a2(v, _) with a1.stmt# != a2.stmt#

5000
31 - 2 pattern + next + uses
procedure p; assign a; if ifs; variable v;
Select a such that Next(a, ifs) pattern ifs("x", _, _) pattern ifs("y", _, _) such that Uses(ifs, v)

5000
32 - 2 pattern + next + uses
procedure p; assign a; if ifs; variable v;
Select <ifs, v> such that Next(a, ifs) pattern ifs("x", _, _) pattern ifs("y", _, _) such that Uses(ifs, v)

5000
33 - 2 pattern + parent* + Next
assign a; if ifs; while w; read r;
Select a pattern a("x", _) such that Parent*(w, ifs) pattern a(_, _"a"_) such that Next(w, a)

5000
34 - modifies + uses + 2 pattern
assign a1, a2; if ifs; while w; variable v; call c; print r1, r2;
Select v such that Uses(r1, v) such that Modifies(a1, v) pattern ifs(v, _, _) and a2(v, _)

5000
35 - modifies + uses + 2 pattern
assign a1, a2; if ifs; while w; variable v; call c; print r1, r2;
Select v such that Uses(r1, v) such that Modifies(a1, v) pattern ifs(v, _, _) and a1(v, _)

5000
36 - 2 pattern + parent + parent*
if ifs; while w; procedure p; call c;
Select p pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w, c) and Parent*(w, ifs)

5000
37 - 2 pattern + parent + parent*
if ifs; while w; procedure p; call c;
Select ifs pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w, c) and Parent*(w, ifs)

5000
38 - 2 pattern + parent + parent*
if ifs; while w; procedure p; call c;
Select <w, c> pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w, c) and Parent*(w, ifs)

5000
39 - 2 pattern + follows + parent
if ifs; while w; procedure p; variable v; assign a; print pr;
Select p such that Parent(w, ifs) pattern ifs(v, _, _) such that Follows(a, pr) pattern ifs(_, _, _)

5000
40 - 2 pattern + calls + next
procedure p1, p2; call c; assign a;
Select p2 such that Calls(p1, "proc8") and Next(c, a) pattern a(_, _) and pattern a("a", _)

5000
41 - 2 pattern + calls + next
procedure p1, p2; call c; assign a;
Select <c, a> such that Calls(p1, "proc8") and Next(c, a) pattern a(_, _) and pattern a("a", _)

5000
42 - 2 pattern + follows + parent
if ifs; while w; procedure p; variable v; assign a; print pr;
Select ifs pattern ifs(v, _, _) and ifs(_, _, _) such that Parent(ifs, w) and Follows(a, pr)

5000
43 - 2 pattern + calls + next
procedure p1; assign a; call c;
Select a such that Calls(p1, "proc9") and Next(c, a) pattern a(_, _"19"_) and a("c", _)

5000
44 - 2 pattern + calls + next
procedure p1; assign a; call c;
Select p1 such that Calls(p1, "proc9") and Next(c, a) pattern a(_, _"19"_) and a("c", _)

5000
45 - 2 pattern + calls + next
procedure p1; assign a; call c;
Select c such that Calls(p1, "proc9") and Next(c, a) pattern a(_, _"19"_) and a("c", _)

5000
46 - 2 pattern + calls + next
procedure p1; assign a; call c;
Select <a, c, p1> such that Calls(p1, "proc9") and Next(c, a) pattern a(_, _"19"_) and a("c", _)

5000
47 - 2 pattern + parent + parent*
if ifs; procedure p; call c; while w1, w2;
Select w2 pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w1, c) and Parent*(ifs, w1)

5000
48 - 2 pattern + parent + parent* + with
if ifs; procedure p; call c; while w1, w2;
Select w2 pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w1, c) and Parent*(ifs, w1) with w1.stmt# != w2.stmt#

5000
49 - 2 pattern + parent + parent*
if ifs; procedure p; call c; while w1, w2;
Select ifs pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w1, c) and Parent*(ifs, w1)

5000
50 - 2 pattern + parent + parent*
if ifs; procedure p; call c; while w1, w2;
Select <w1, w2> pattern ifs("x", _, _) and ifs("y", _, _) such that Parent(w1, c) and Parent*(ifs, w1)

5000
51 - 2 pattern + parent + follows
if ifs; procedure p; variable v1, v2; while w; assign a; print pr;
Select w such that Parent(ifs, w) pattern ifs(v1, _, _) such that Follows(a, pr) pattern ifs(v2, _, _)

5000
52 - 2 pattern + parent + follows + with
if ifs; procedure p; variable v1, v2; while w; assign a; print pr;
Select w such that Parent(ifs, w) pattern ifs(v1, _, _) such that Follows(a, pr) pattern ifs(v2, _, _) with v1.varName != v2.varName

5000
53 - 2 pattern + parent + follows
if ifs; procedure p; variable v1, v2; while w; assign a; print pr;
Select <v1, v2> such that Parent(ifs, w) pattern ifs(v1, _, _) such that Follows(a, pr) pattern ifs(v2, _, _)

5000
54 - 2 pattern + follows + parent
if ifs; while w; procedure p; variable v; assign a; print pr;
Select p such that Parent(ifs, w) pattern ifs(v, _, _) and a("a", _) such that Follows(a, pr)

5000
55 - 2 pattern + follows + parent
if ifs; while w; procedure p; variable v; assign a; print pr;
Select v such that Parent(ifs, w) pattern ifs(v, _, _) and a("a", _) such that Follows(a, pr)

5000
56 - 2 pattern + follows + parent
if ifs; while w; procedure p; variable v; assign a; print pr;
Select <ifs, w> such that Parent(ifs, w) pattern ifs(v, _, _) and a("a", _) such that Follows(a, pr)

5000
57 - 2 pattern + follows + next
assign a; if ifs1, ifs2; print pr; procedure p;
Select p such that Follows(a, ifs1) such that Next*(pr, ifs2) pattern a(_, _"x"_) and ifs2(_, _, _)

5000
58 - 2 pattern + follows + next
assign a; if ifs1, ifs2; print pr; procedure p;
Select pr such that Follows(a, ifs1) such that Next*(pr, ifs2) pattern a(_, _"x"_) and ifs2(_, _, _)

5000
59 - 2 pattern + calls + uses
procedure p1, p2; variable v; assign a; call c;
Select c such that Calls*(p1, p2) and Uses(p1, v) pattern a("x", _) pattern a(_, _"x"_)

5000
60 - 2 pattern + follows + next
if ifs1, ifs2; assign a; print p;
Select p such that Follows(a, ifs1) such that Next(p, ifs1) pattern a(_, _"x"_) and ifs1(_, _, _)

5000
61 - 2 pattern + follows + next
if ifs1, ifs2; assign a; print p;
Select a such that Follows(a, ifs1) such that Next(p, ifs1) pattern a(_, _"x"_) and ifs1(_, _, _)

5000
62 - 2 pattern + follows + next
if ifs1, ifs2; assign a; print p;
Select <ifs1, ifs2> such that Follows(a, ifs1) such that Next(p, ifs1) pattern a(_, _"x"_) and ifs2(_, _, _)

5000
63 - 2 pattern + calls + uses
procedure p1, p2; assign a; variable v; call c;
Select p1 such that Calls*(p1, p2) and Uses(p1, v) pattern a("x", _) and a(_, _"y"_)

5000
64 - 2 pattern + calls + uses
procedure p1, p2; assign a; variable v; call c;
Select <p1, p2> such that Calls*(p1, p2) and Uses(p1, v) pattern a("x", _) and a(_, _"y"_)

5000
65 - 2 pattern + calls + uses
procedure p1, p2; assign a; variable v; call c;
Select <a, v> such that Calls*(p1, p2) and Uses(p1, v) pattern a("x", _) and a(_, _"y"_)

5000
66 - 2 pattern + parent + follows
if ifs; procedure p; variable v; while w; assign a; print pr;
Select a such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, pr) pattern w("z", _)

5000
67 - 2 pattern + parent + follows
if ifs; procedure p; variable v; while w; assign a; print pr;
Select p such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, pr) pattern w("z", _)

5000
68 - 2 pattern + parent + follows
if ifs; procedure p; variable v; while w; assign a; print pr;
Select w such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, pr) pattern w("z", _)

5000
69 - 2 pattern + parent + follows
if ifs; procedure p; variable v; while w; assign a; print pr;
Select <ifs, w> such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, pr) pattern w("z", _)

5000
70 - 2 pattern + parent + follows
if ifs; procedure p; variable v; while w; assign a; print pr;
Select <ifs, w> such that Parent(w, ifs) pattern ifs(v, _, _) such that Follows(a, pr) pattern w("z", _)

5000
71 - 2 pattern + follows + uses
assign a; if ifs1; print p;
Select ifs1 such that Follows(a, ifs1) such that Uses(ifs1, "a") pattern a(_, _"y"_) and ifs1("x", _, _)

5000
72 - 2 pattern + follows + uses
assign a; if ifs1; print p;
Select <a, p> such that Follows(a, ifs1) such that Uses(ifs1, "a") pattern a(_, _"y"_) and ifs1("x", _, _)

5000
73 - 2 pattern + follows + uses
assign a; if ifs1; print p;
Select <ifs1, p, a> such that Follows(a, ifs1) such that Uses(ifs1, "a") pattern a(_, _"y"_) and ifs1("x", _, _)

5000
74 - 2 pattern + Follows + uses
assign a; if ifs1, ifs2; call c; read r;
Select c such that Follows(a, r) such that Uses(ifs1, "a") pattern a(_, _"c"_) and ifs2("x", _, _)

5000
75 - 2 pattern + Follows + uses
assign a; if ifs1, ifs2; call c; read r;
Select a such that Follows(a, r) such that Uses(ifs1, "a") pattern a(_, _"c"_) and ifs2("x", _, _)

5000
76 - 2 pattern + Follows + uses
assign a; if ifs1, ifs2; call c; read r;
Select r such that Follows(a, r) such that Uses(ifs1, "a") pattern a(_, _"c"_) and ifs2("x", _, _)

5000
77 - 2 pattern + Follows + uses
assign a; if ifs1, ifs2; call c; read r;
Select <c, a, r> such that Follows(a, r) such that Uses(ifs1, "a") pattern a(_, _"c"_) and ifs2("x", _, _)

5000
78 - 2 pattern + calls + uses
procedure p; while w; variable v; assign a; call c;
Select c such that Calls*(p, "proc9") and Uses(p, v) pattern w("x", _) and a(_, _"x"_)

5000
79 - 2 pattern + calls + uses
procedure p; while w; variable v; assign a; call c;
Select <p, v> such that Calls*(p, "proc9") and Uses(p, v) pattern w("x", _) and a(_, _"x"_)

5000
80 - 2 pattern + calls + uses
procedure p; while w; variable v; assign a; call c;
Select w such that Calls*(p, "proc9") and Uses(p, v) pattern w("x", _) and a(_, _"x"_)

5000
81 - 2 pattern + parent + follows
if ifs; while w1, w2; variable v; assign a; print p;
Select ifs such that Parent(ifs, w1) pattern ifs(v, _, _) such that Follows(a, p) pattern w2("x", _)

5000
82 - 2 pattern + parent + follows
if ifs; while w1, w2; variable v; assign a; print p;
Select <w1, w2> such that Parent(ifs, w1) pattern ifs(v, _, _) such that Follows(a, p) pattern w2("x", _)

5000
83 - 2 pattern + parent + follows
if ifs; while w1, w2; variable v; assign a; print p;
Select w1 such that Parent(ifs, w1) pattern ifs(v, _, _) such that Follows(a, p) pattern w2("w", _)

5000
84 - 2 pattern + parent + follows
if ifs; while w1, w2; variable v; assign a; print p;
Select <w1, w2> such that Parent(ifs, w1) pattern ifs(v, _, _) such that Follows(a, p) pattern w2("w", _)

5000
85 - 2 pattern + parent + follows
if ifs; while w1, w2; variable v; assign a; print p;
Select ifs such that Parent(ifs, w1) pattern ifs(v, _, _) such that Follows(a, p) pattern w2("w", _)

5000
86 - 2 pattern + parent + follows
if ifs; while w1, w2; variable v; assign a; print p;
Select <a, p> such that Parent(ifs, w1) pattern ifs(v, _, _) such that Follows(a, p) pattern w2("w", _)

5000
87 - 2 pattern + follows + uses
if ifs1, ifs2; assign a; read r; call c;
Select ifs1 such that Follows(r, a) such that Uses(ifs1, "x") pattern a(_, _"x"_) and ifs2("x", _, _)

5000
88 - 2 pattern + follows + uses
if ifs1, ifs2; assign a; read r; call c;
Select <ifs1, ifs2> such that Follows(r, a) such that Uses(ifs1, "x") pattern a(_, _"x"_) and ifs2("x", _, _)

5000
89 - 2 pattern + follows + uses
if ifs1, ifs2; assign a; read r; call c;
Select <a, r> such that Follows(r, a) such that Uses(ifs1, "x") pattern a(_, _"x"_) and ifs2("x", _, _)

5000
90 - 2 pattern + calls* + uses
procedure p; while w; assign a; call c; variable v;
Select w such that Calls*(p, "proc9") and Uses(p, v) pattern w("x", _) and a(_, _"z"_)

5000
91 - 2 pattern + calls* + uses
procedure p; while w; assign a; call c; variable v;
Select <p, a, v> such that Calls*(p, "proc9") and Uses(p, v) pattern w("x", _) and a(_, _"z"_)

5000
92 - 2 pattern + parent + follows
while w; variable v; assign a; print p; if ifs;
Select w such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, p) pattern w(v, _)

5000
93 - 2 pattern + parent + follows
while w; variable v; assign a; print p; if ifs;
Select ifs such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, p) pattern w(v, _)

5000
94 - 2 pattern + parent + follows
while w; variable v; assign a; print p; if ifs;
Select <p, a, v> such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, p) pattern w(v, _)

5000
95 - 2 pattern + modifies + uses
if ifs1; read r; assign a; call c; variable v;
Select ifs1 such Modifies(a, v) such that Uses(ifs1, v) pattern a(_, _"w"_) and ifs1("x", _, _)

5000
96 - 2 pattern + modifies + uses
if ifs1; read r; assign a; call c; variable v;
Select <ifs1, ifs2> such Modifies(a, v) such that Uses(ifs1, v) pattern a(_, _"w"_) and ifs1("x", _, _)

5000
97 - 2 pattern + modifies + uses
if ifs1; read r; assign a; call c; variable v;
Select <a, v> such Modifies(a, v) such that Uses(ifs1, v) pattern a(_, _"w"_) and ifs1("x", _, _)

5000
98 - 2 pattern + next + uses
procedure p; while w; assign a; call c; variable v;
Select w such that Next(w, c) and Uses(a, v) pattern w(v, _) and a(v, _"y"_)

5000
99 - 2 pattern + next + uses
procedure p; while w; assign a; call c; variable v;
Select <a, v> such that Next(w, c) and Uses(a, v) pattern w(v, _) and a(v, _"y"_)

5000
100 - 2 pattern + follows + parent
if ifs; while w; variable v; assign a; print p;
Select a such that Parent(ifs, w) pattern ifs(v, _, _) such that Follows(a, p) pattern w(v, _)

5000
101 - 2 pattern + modifies + uses
assign a; if ifs; call c; variable v1, v2;
Select v1 such that Modifies(a, v1) such that Uses(ifs, v1) pattern a(v2, _"b"_) and ifs("x", _, _)

5000
102 - 2 pattern + modifies + uses
assign a; if ifs; call c; variable v1, v2;
Select <v1, v2> such that Modifies(a, v1) such that Uses(ifs, v1) pattern a(v2, _"b"_) and ifs("x", _, _)

5000
103 - 2 pattern + uses + modifies
variable v1, v2; while w; assign a;
Select v1 such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v1, _)

5000
104 - 2 pattern + uses + modifies
variable v1, v2; while w; assign a;
Select <v1, w> such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v1, _)

5000
105 - 2 pattern + uses + modifies
variable v1, v2; while w; assign a;
Select <v1, v2> such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v1, _)

5000
106 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w; if ifs;
Select v1 such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a2(v2, _)

5000
107 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w; if ifs;
Select <v1, v2> such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a2(v2, _)

5000
108 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w; if ifs;
Select <a1, a2> such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a2(v2, _)

5000
109 - 2 pattern + uses + modifies
variable v1, v2; assign a; if ifs; while w;
Select ifs pattern a(v1, _) and ifs(v2, _, _) such that Uses(a, v1) and Modifies(ifs, v2)

5000
110 - 2 pattern + uses + modifies
variable v1, v2; assign a; if ifs; while w;
Select <v1, v2> pattern a(v1, _) and ifs(v2, _, _) such that Uses(a, v1) and Modifies(ifs, v2)

5000
111 - 2 pattern + uses + modifies
variable v1, v2; assign a; if ifs; while w;
Select a pattern a(v1, _) and ifs(v2, _, _) such that Uses(a, v1) and Modifies(ifs, v2)

5000
112 - 2 pattern + uses + modifies
variable v1, v2; while w; call c; assign a;
Select c such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v1, _)

5000
113 - 2 pattern + uses + modifies
variable v1, v2; while w; call c; assign a;
Select c such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v2, _)

5000
114 - 2 pattern + uses + modifies
variable v1, v2; while w; call c; assign a;
Select <v1, v2> such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v1, _)

5000
115 - 2 pattern + uses + modifies
variable v1, v2; while w; call c; assign a;
Select <v1, v2> such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v2, _)

5000
116 - 2 pattern + uses + modifies
variable v1, v2; while w; call c; assign a;
Select <a, w> such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v1, _)

5000
117 - 2 pattern + uses + modifies
variable v1, v2; while w; call c; assign a;
Select <a, w> such that Uses(a, v1) and Modifies(a, v1) pattern w(v2, _) and w(v2, _)

5000
118 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w;  procedure p;
Select p such that Uses(a1, v1) and Modifies(a1, 1v) pattern a2(v2, _) and a2(v2, _)

5000
119 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2;
Select <a1, a2> such that Uses(a1, v1) and Modifies(a1, 1v) pattern a2(v2, _) and a2(v2, _)

5000
120 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2;
Select <v1, v2> such that Uses(a1, v1) and Modifies(a1, 1v) pattern a2(v2, _) and a2(v2, _)

5000
121 - 2 pattern + uses + modifies
variable v1, v2; assign a; while w; if ifs; call c;
Select c pattern a(v1, _) and ifs(v2, _, _) such that Uses(a, v1) and Modifies(ifs, v2)

5000
122 - 2 pattern + uses + modifies
variable v1, v2; assign a; while w; if ifs;
Select ifs pattern a(v1, _) and ifs(v2, _, _) such that Uses(a, v1) and Modifies(ifs, v2)

5000
123 - 2 pattern + uses + modifies
variable v1, v2; assign a; while w; if ifs;
Select <v1, v2> pattern a(v1, _) and ifs(v2, _, _) such that Uses(a, v1) and Modifies(ifs, v2)

5000
124 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w1;
Select v1 such that Uses(a1, v1) and Modifies(a1, v1) pattern w1(v2, _) and a2(v2, _)

5000
125 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w1;
Select <a1, v1> such that Uses(a1, v1) and Modifies(a1, v1) pattern w1(v2, _) and a2(v2, _)

5000
126 - 2 pattern + uses + modifies
variable v1, v2; assign a1, a2; while w1;
Select <a2, v2> such that Uses(a1, v1) and Modifies(a1, v1) pattern w1(v2, _) and a2(v2, _)

5000
127 - 2 pattern + uses + modifies
variable v1, v2, v3; assign a1, a2, a3;
Select v1 such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a3(v3, _)

5000
128 - 2 pattern + uses + modifies
variable v1, v2, v3; assign a1, a2, a3;
Select <v1, v2, v3> such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a3(v3, _)

5000
129 - 2 pattern + uses + modifies
variable v1, v2, v3; assign a1, a2, a3;
Select <a1, a2, a3> such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a3(v3, _)

5000
130 - 2 pattern + uses + modifies
variable v1, v2, v3; assign a1, a2, a3;
Select a2 such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a3(v3, _"a"_)

5000
131 - 2 pattern + uses + modifies
variable v1, v2, v3; assign a1, a2, a3;
Select <a1, a2> such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a3(v3, _"a"_)

5000
132 - 2 pattern + uses + modifies
variable v1, v2, v3; assign a1, a2, a3;
Select <a3, v2> such that Uses(a1, v1) and Modifies(a1, v1) pattern a2(v2, _) and a3(v3, _"a"_)

5000
133 - 2 pattern + uses + modifies
variable v; assign a; procedure p;
Select p such that Uses(a, v) and Modifies(a, v) pattern a(v, _) and a(v, _)

5000
134 - 2 pattern + uses + modifies
variable v; assign a; procedure p;
Select <p, a, v> such that Uses(a, v) and Modifies(a, v) pattern a(v, _) and a(v, _)

5000
135 - 2 pattern + uses + modifies
variable v; assign a;
Select <a, v> such that Uses(a, v) and Modifies(a, v) pattern a(v, _) and a(v, _)

5000
136 - 2 pattern + uses + modifies
variable v; assign a; call c;
Select c such that Uses(a, v) and Modifies(a, v) pattern a(v, _"x"_) and a(v, _"y"_)

5000
137 - 2 pattern + uses + modifies
variable v; assign a; call c;
Select a such that Uses(a, v) and Modifies(a, v) pattern a(v, _"x"_) and a(v, _"y"_)

5000
138 - 2 pattern + uses + modifies
variable v; assign a; call c;
Select v such that Uses(a, v) and Modifies(a, v) pattern a(v, _"x"_) and a(v, _"y"_)

5000
139 - 2 pattern + uses + modifies
variable v; assign a; call c;
Select <a, v> such that Uses(a, v) and Modifies(a, v) pattern a(v, _"x"_) and a(v, _"y"_)

5000
140 - six affects
assign a1, a2, a3, a4, a5, a6, a7, a8, a9, a10, a11, a12; stmt s;
Select s such that Affects(a1, a2) such that Affects(a3, a4) such that Affects(a5, a6) such that Affects(a7, a8) such that Affects(a9, a10) such that Affects(a11, a12)

5000
141 - six ffects*
assign a1, a2, a3, a4, a5, a6, a7, a8, a9, a10, a11, a12; read r;
Select r such that Affects*(a1, a2) such that Affects*(a3, a4) such that Affects*(a5, a6) such that Affects*(a7, a8) such that Affects*(a9, a10) such that Affects*(a11, a12)

5000
142 - six next*
stmt n1, n2, n3, n4, n5, n6, n7, n8, n9, n10, n11, n12;
Select n6 such that Next*(n1, n2) such that Next*(n3, n4) such that Next*(n5, n6) such that Next*(n7, n8) such that Next*(n9, n10) such that Next*(n11, n12)

5000
143 - six affects*
assign a1, a2, a3, a4, a5, a6, a7, a8, a9, a10, a11, a12;
Select <a12, a6> such that Affects*(a1, a2) such that Affects*(a3, a4) such that Affects*(a5, a6) such that Affects*(a7, a8) such that Affects*(a9, a10) such that Affects*(a11, a12)

5000
144 - six next*
stmt n1, n2, n3, n4, n5, n6, n7, n8, n9, n10, n11, n12; procedure p; variable v;
Select <v.varName,p> such that Next*(n1, n2) such that Next*(n3, n4) such that Next*(n5, n6) such that Next*(n7, n8) such that Next*(n9, n10) such that Next*(n11, n12)

5000
145 - six next* (s,s)
stmt n1, n2; while w; if ifs; assign a; call c;
Select <ifs,c,w> such that Next*(n1, n1) such that Next*(n2, n2) such that Next*(w, w) such that Next*(ifs, ifs) such that Next*(a, a) such that Next*(c, c)

5000
146 - 3 modifies, 3 uses
variable v1, v2, v3; while w; if ifs; procedure p1, p2, p3;
Select <w, v3> such that Uses(w, v1) and Uses(ifs, v1) and Uses(p1, v1) such that Modifies(p2 ,v2) such that Modifies(p3, v3) and Modifies(p1, v1)

5000
147 - 3 parent, 3 follows
stmt s1, s2, s3; variable v1; while w; if ifs; procedure p1, p2, p3;
Select <s1, w, ifs, s2> such that Parent(s1, ifs) and Parent*(s2, ifs) and Parent*(s2, w) and Follows*(185, 187) and Follows(s1,_) and Follows*(_,_)

5000
148 - 4 calls, 1 Uses, 1 Modifies
call c1, c2; variable v1; procedure p1, p2, p3;
Select <c1, c2, p3, v1, c1> such that Modifies(" patternHelper",v1) such that Calls*(p1,_) and Calls(_,p2) and Calls*(p3,"longHelper") and Calls*("main","oneCallHelper") such that Uses("p ",v1)

5000
