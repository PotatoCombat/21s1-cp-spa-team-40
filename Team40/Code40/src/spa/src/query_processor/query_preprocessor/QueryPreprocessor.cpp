#include "QueryPreprocessor.h"

Query *QueryPreprocessor::preprocessQuery(string input) {
    // read the input
    // tokenize the input using a lexer
    // syntax checker here
    // parse the tokens using parser
    // should return the entity / relationship items as in the component model
    // class
    // semantics checker here (?)
    return Query();
}